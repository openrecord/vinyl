// Actions

// Initial State
const INITIAL_STATE = {
	user: null
};

// Reducer
export default function reducer(state = INITIAL_STATE, action = {}) {
	switch (action.type) {
		default:
			return state;
	}
}

// Action Creators
